{"code":"// security_alarm.ino\n// ELEGOO UNO R3 + HC-SR04 + Buzzer\n\nconst int TRIG_PIN = 9;\nconst int ECHO_PIN = 10;\nconst int BUZZER_PIN = 8;\n\nvoid setup() {\n  pinMode(TRIG_PIN, OUTPUT);\n  pinMode(ECHO_PIN, INPUT);\n  pinMode(BUZZER_PIN, OUTPUT);\n\n  Serial.begin(9600);\n}\n\nvoid loop() {\n  // Send an ultrasonic ping\n  digitalWrite(TRIG_PIN, LOW);\n  delayMicroseconds(2);\n\n  digitalWrite(TRIG_PIN, HIGH);\n  delayMicroseconds(10);\n\n  digitalWrite(TRIG_PIN, LOW);\n\n  // Wait for the echo\n  long duration = pulseIn(ECHO_PIN, HIGH, 30000);\n\n  // If we didn't receive an echo, don't trigger the alarm\n  if (duration == 0) {\n    noTone(BUZZER_PIN);\n    Serial.println(\"Nothing detected\");\n    delay(100);\n    return;\n  }\n\n  // Calculate distance\n  float distance = duration * 0.0343 / 2;\n\n  Serial.print(\"Distance: \");\n  Serial.print(distance);\n  Serial.println(\" cm\");\n\n  // SECURITY ALARM\n  if (distance <= 15) {\n    // Something is within 15 cm!\n    tone(BUZZER_PIN, 1000);\n  } else {\n    // Area is clear\n    noTone(BUZZER_PIN);\n  }\n\n  delay(100);\n}","files":[],"features":{"smartWifi":false}}