{"code":"#include <Arduino.h>\n#include <RF24.h>\n#include <SPI.h>\n#include <esp_bt.h>\n#include <esp_wifi.h>\n#include <esp_bt_main.h>\n\n// Kutubxonalar\n#include <Wire.h>\n#include <Adafruit_GFX.h>\n#include <Adafruit_SSD1306.h>\n#include <MFRC522.h>\n\n// OLED Ekran sozlamalari\n#define SCREEN_WIDTH 128\n#define SCREEN_HEIGHT 64\nAdafruit_SSD1306 display(SCREEN_WIDTH, SCREEN_HEIGHT, &Wire, -1);\n\n// RC522 RFID sozlamalari\n#define RST_PIN         26\n#define SS_PIN          25\nMFRC522 mfrc522(SS_PIN, RST_PIN);\nMFRC522::MIFARE_Key key;\n\n// Rejim tugmasi (ICHKI PULL-UP BOR 14-PIN GA BELGILANDI)\n#define BUTTON_PIN      14 \nenum DeviceMode { MODE_JAMMER, MODE_RFID_MENU, MODE_RFID_READ, MODE_RFID_WRITE };\nDeviceMode currentMode = MODE_JAMMER;\n\n// Tugma bosilishini aniqlash (Debounce) o'zgaruvchilari\nunsigned long lastDebounceTime = 0;\nunsigned long debounceDelay = 250; \nint lastButtonState = HIGH;\n\n// RFID ma'lumotlari buferi\nbyte storedUID[4];\nbool hasStoredCard = false;\n\n// NRF24 o'zgaruvchilari (Sizning asl kodingiz)\nSPIClass *sp = nullptr;\nRF24 radioA(5, 17, 19909090);\nRF24 radioB(16, 4, 19909090);\n\nint HSPIchannel = 45;\nint VSPIchannel = 45;\nunsigned int HSPIflag = 0;\nunsigned int VSPIflag = 0;\n\nvoid channelSweeper() {\n  if (VSPIflag == 0) VSPIchannel += 4; else VSPIchannel -= 4;\n  if (HSPIflag == 0) HSPIchannel += 2; else HSPIchannel -= 2;\n  if ((VSPIchannel > 79) && (VSPIflag == 0)) VSPIflag = 1; else if ((VSPIchannel < 2) && (VSPIflag == 1)) VSPIflag = 0;\n  if ((HSPIchannel > 79) && (HSPIflag == 0)) HSPIflag = 1; else if ((HSPIchannel < 2) && (HSPIflag == 1)) HSPIflag = 0;\n  radioA.setChannel(HSPIchannel);\n  radioB.setChannel(VSPIchannel);\n}\n\nvoid randomChannel() {\n  radioA.setChannel(random(80));\n  radioB.setChannel(random(80));\n  delayMicroseconds(random(60));\n}\n\nvoid initHSPI() {\n  sp = new SPIClass(VSPI);\n  sp->begin(18, 19, 23);\n  if (radioA.begin(sp)) {\n    radioA.setAutoAck(false);\n    radioA.stopListening();\n    radioA.setRetries(0, 0);\n    radioA.setPayloadSize(32);\n    radioA.setAddressWidth(5);\n    radioA.setPALevel(RF24_PA_MAX, true);\n    radioA.setDataRate(RF24_2MBPS);\n    radioA.setCRCLength(RF24_CRC_DISABLED);\n    radioA.startConstCarrier(RF24_PA_MAX, HSPIchannel);\n  }\n}\n\nvoid initVSPI() {\n  if (radioB.begin(sp)) {\n    radioB.setAutoAck(false);\n    radioB.stopListening();\n    radioB.setRetries(0, 0);\n    radioB.setPayloadSize(32);\n    radioB.setAddressWidth(5);\n    radioB.setPALevel(RF24_PA_MAX, true);\n    radioB.setDataRate(RF24_2MBPS);\n    radioB.setCRCLength(RF24_CRC_DISABLED);\n    radioB.startConstCarrier(RF24_PA_MAX, VSPIchannel);\n  }\n}\n\n// Ekran yangilash funksiyasi\nvoid updateDisplay() {\n  display.clearDisplay();\n  display.setTextSize(1);\n  display.setTextColor(SSD1306_WHITE);\n  display.setCursor(0, 0);\n  \n  display.println(\"=== MULTI-TOOL OS ===\");\n  display.println();\n\n  if (currentMode == MODE_JAMMER) {\n    display.setTextSize(2);\n    display.println(\"JAMMER: ON\");\n    display.setTextSize(1);\n    display.println(\"Freq: 2.4 GHz Sweep\");\n  } \n  else if (currentMode == MODE_RFID_MENU) {\n    display.println(\"> RFID Duplicator\");\n    display.println(\"  [Press to Enter]\");\n  }\n  else if (currentMode == MODE_RFID_READ) {\n    display.println(\"Mode: RFID READ\");\n    display.println(\"Karta yaqinlashtiring...\");\n    if (hasStoredCard) {\n      display.print(\"Saqlangan: \");\n      for (byte i = 0; i < 4; i++) {\n        display.print(storedUID[i], HEX);\n        display.print(\" \");\n      }\n    }\n  }\n  else if (currentMode == MODE_RFID_WRITE) {\n    display.println(\"Mode: RFID WRITE\");\n    if (hasStoredCard) {\n      display.println(\"UID yuklangan.\");\n      display.println(\"Yangi kartani qo'ying.\");\n    } else {\n      display.println(\"Xatolik: Avval kartani\");\n      display.println(\"o'qib oling!\");\n    }\n  }\n  display.display();\n}\n\n// Rejimlarni xavfsiz va ketma-ket almashtirish\nvoid changeMode() {\n  if (currentMode == MODE_JAMMER) currentMode = MODE_RFID_MENU;\n  else if (currentMode == MODE_RFID_MENU) currentMode = MODE_RFID_READ;\n  else if (currentMode == MODE_RFID_READ) currentMode = MODE_RFID_WRITE;\n  else if (currentMode == MODE_RFID_WRITE) currentMode = MODE_JAMMER;\n  \n  updateDisplay();\n}\n\n// Tugmani tinimsiz aylanib ketishdan asrovchi tekshirish funksiyasi\nvoid checkButton() {\n  int reading = digitalRead(BUTTON_PIN);\n\n  // Agar tugma holati o'zgargan bo'lsa (bosilgan bo'lsa)\n  if (reading == LOW && lastButtonState == HIGH) {\n    if ((millis() - lastDebounceTime) > debounceDelay) {\n      changeMode();\n      lastDebounceTime = millis();\n    }\n  }\n  lastButtonState = reading;\n}\n\n// RFID Kartani o'qish\nvoid handleRFIDRead() {\n  if (!mfrc522.PICC_IsNewCardPresent() || !mfrc522.PICC_ReadCardSerial()) {\n    return;\n  }\n  \n  for (byte i = 0; i < 4; i++) {\n    storedUID[i] = mfrc522.uid.uidByte[i];\n  }\n  hasStoredCard = true;\n  \n  display.clearDisplay();\n  display.setCursor(0, 0);\n  display.println(\"=== RFID READ ===\");\n  display.println(\"Karta muvaffaqiyatli\");\n  display.println(\"o'qildi va saqlandi!\");\n  display.display();\n  delay(1500);\n  \n  currentMode = MODE_RFID_MENU; \n  updateDisplay();\n}\n\n// RFID Kartaga yozish (Klonlash)\nvoid handleRFIDWrite() {\n  // Agar xotirada karta bo'lmasa qotib qolmasdan avtomatik Jammerga qaytadi\n  if (!hasStoredCard) {\n    display.clearDisplay();\n    display.setCursor(0, 0);\n    display.println(\"XATOLIK:\");\n    display.println(\"Saqlangan UID yo'q!\");\n    display.println(\"Jammerga qaytilmoqda...\");\n    display.display();\n    delay(2000);\n    currentMode = MODE_JAMMER;\n    updateDisplay();\n    return;\n  }\n\n  if (!mfrc522.PICC_IsNewCardPresent() || !mfrc522.PICC_ReadCardSerial()) {\n    return;\n  }\n\n  byte blockAddr = 0;\n  byte trailerBlock = 3;\n  MFRC522::StatusCode status;\n\n  for (byte i = 0; i < 6; i++) key.keyByte[i] = 0xFF;\n\n  status = mfrc522.PCD_Authenticate(MFRC522::PICC_CMD_MF_AUTH_KEY_A, trailerBlock, &key, &(mfrc522.uid));\n  if (status != MFRC522::STATUS_OK) {\n     display.clearDisplay();\n     display.println(\"Auth Xatolik!\");\n     display.display();\n     delay(1000);\n     return;\n  }\n\n  byte buffer[16];\n  byte size = sizeof(buffer);\n  \n  status = mfrc522.MIFARE_Read(blockAddr, buffer, &size);\n  \n  for(int i=0; i<4; i++) {\n    buffer[i] = storedUID[i];\n  }\n  buffer[4] = buffer[0] ^ buffer[1] ^ buffer[2] ^ buffer[3]; \n\n  status = mfrc522.MIFARE_Write(blockAddr, buffer, 16);\n  if (status == MFRC522::STATUS_OK) {\n    display.clearDisplay();\n    display.println(\"Klonlash bajarildi!\");\n    display.display();\n    delay(2000);\n    currentMode = MODE_JAMMER; \n  } else {\n    display.clearDisplay();\n    display.println(\"Yozishda xatolik!\");\n    display.display();\n    delay(2000);\n  }\n  updateDisplay();\n}\n\nvoid setup() {\n  // P14 pinida ichki pull-up rezistorni faollashtirish\n  pinMode(BUTTON_PIN, INPUT_PULLUP);\n\n  // OLED yuklash\n  if(!display.begin(SSD1306_SWITCHCAPVCC, 0x3C)) { \n    for(;;); \n  }\n  \n  display.clearDisplay();\n  display.setTextSize(1);\n  display.setTextColor(SSD1306_WHITE);\n  display.setCursor(0, 20);\n  display.println(\"Tizim yuklanmoqda...\");\n  display.display();\n\n  // ESP32 ichki simsiz modullarini o'chirish (Sizning kodingiz)\n  esp_bt_controller_deinit();\n  esp_wifi_stop();\n  esp_wifi_deinit();\n  esp_wifi_disconnect();\n  if (esp_bluedroid_get_status() == ESP_BLUEDROID_STATUS_ENABLED) {\n    esp_bluedroid_deinit();\n    esp_bluedroid_disable();\n  }\n  \n  // NRF modullarini ishga tushirish (Sizning kodingiz)\n  initHSPI();\n  initVSPI();\n\n  // RC522 RFID modulini yuklash\n  mfrc522.PCD_Init();\n\n  // Ekranni birinchi holatga o'tkazish\n  updateDisplay();\n}\n\nvoid loop() {\n  // 1. Tugmani doimiy va birinchi navbatda tekshiramiz\n  checkButton();\n\n  // 2. Rejimlar boshqaruvi\n  if (currentMode == MODE_JAMMER) {\n    channelSweeper();\n    randomChannel();\n    // Jammer o'ta agressiv ishlaganda tugma algoritmi adashmasligi uchun 5 mikrosekund nafas beramiz\n    delayMicroseconds(5); \n  } \n  else if (currentMode == MODE_RFID_READ) {\n    handleRFIDRead();\n  }\n  else if (currentMode == MODE_RFID_WRITE) {\n    handleRFIDWrite();\n  }\n}","files":[],"features":{"smartWifi":true},"smartWifi":{"enabled":true,"apSsid":"ESP32-Setup","apPass":"","portalTitle":"ESP32 Wi-Fi Setup","accent":"#00e676","timeoutSeconds":60}}