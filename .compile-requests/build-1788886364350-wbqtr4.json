{"code":"const int ledPin = 13;     // Порт для светодиода\nconst int buzzerPin = 12;   // Порт для пищалки\nconst int buttonPin = 2;    // Порт для кнопки\n\nvoid setup() {\n  pinMode(ledPin, OUTPUT);\n  pinMode(buzzerPin, OUTPUT);\n  \n  // Включаем встроенный подтягивающий резистор для кнопки. \n  // Это значит, что при нажатии кнопка будет замыкать порт D2 на GND.\n  pinMode(buttonPin, INPUT_PULLUP); \n}\n\nvoid loop() {\n  // Ждем, пока кнопка будет нажата (сигнал станет LOW)\n  while (digitalRead(buttonPin) == HIGH) {\n    delay(10); // Ждем нажатия кнопки, ничего не делая\n  }\n\n  // --- КНОПКА НАЖАТА! НАЧИНАЕТСЯ ОТСЧЕТ 10 СЕКУНД ---\n\n  // 1. Первые 7 секунд — редкие пики и вспышки\n  for (int i = 0; i < 7; i++) {\n    digitalWrite(ledPin, HIGH);\n    digitalWrite(buzzerPin, HIGH);\n    delay(150); \n    digitalWrite(ledPin, LOW);\n    digitalWrite(buzzerPin, LOW);\n    delay(850); \n  }\n\n  // 2. Следующие 2 секунды — учащенный ритм\n  for (int i = 0; i < 4; i++) {\n    digitalWrite(ledPin, HIGH);\n    digitalWrite(buzzerPin, HIGH);\n    delay(100);\n    digitalWrite(ledPin, LOW);\n    digitalWrite(buzzerPin, LOW);\n    delay(400);\n  }\n\n  // 3. 10-я секунда — Финал (постоянный свет и непрерывный писк)\n  digitalWrite(ledPin, HIGH);\n  digitalWrite(buzzerPin, HIGH);\n\n  // Полная остановка программы, пока не нажмешь Reset на самой плате\n  while(true) {\n    // Взрыв произошел\n","files":[],"features":{"smartWifi":false}}